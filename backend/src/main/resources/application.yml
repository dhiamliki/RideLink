spring:
  application:
    name: ridelink-backend
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5433}/${DB_NAME:ridelink}
    username: ${DB_USER:ridelink}
    password: ${DB_PASSWORD:ridelink}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    locations: classpath:db/migration

server:
  port: 8080
